// Firebase has been replaced by Supabase.
// This stub exists so any remaining imports don't crash at startup.
// Migrate any remaining references to use lib/supabase.ts instead.
export const app = null;
export const auth = null;
export const db = null;
export const storage = null;
