# CI Pipeline Verification

This file exists solely to trigger a real pull request against `main` and confirm
that `.github/workflows/ci.yml` actually executes on GitHub Actions — as opposed
to only being reproduced locally.

No application code is affected by this change. Safe to merge or close without
further review once the Actions run has been inspected.

Verification run initiated: 2026-09-18.
